root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "web/dist"]
  delay = 300

[log]
  time = false

[misc]
  clean_on_exit = true
